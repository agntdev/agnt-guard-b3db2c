[
  { "name": "/kick requires a group admin", "steps": [
    { "send": { "text": "/kick @member repeated links" }, "expect": [
      { "method": "sendMessage", "payload": { "text": "This control is available to group admins only." } }
    ] }
  ] }
]
