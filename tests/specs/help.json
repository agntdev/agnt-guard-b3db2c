[
  { "name": "/help explains group moderation", "steps": [
    { "send": { "text": "/help" }, "expect": [
      { "method": "sendMessage", "payload": { "text": "Add GroupGuard as an admin in your group, then use /start to open the moderation controls.\n\nNew members can verify with the button. Group admins can use /warn, /mute, and /kick by replying to a member with a reason." } }
    ] }
  ] },
  { "name": "Help button shows the same guidance", "steps": [
    { "send": { "callback": "menu:help" }, "expect": [
      { "method": "editMessageText", "payload": { "text": "Add GroupGuard as an admin in your group, then use /start to open the moderation controls.\n\nNew members can verify with the button. Group admins can use /warn, /mute, and /kick by replying to a member with a reason." } }
    ] }
  ] }
]
