[
  { "name": "/start opens the GroupGuard menu", "steps": [
    { "send": { "text": "/start" }, "expect": [
      { "method": "sendMessage", "payload": { "text": "GroupGuard keeps your group clear and gives admins fast moderation controls." } }
    ] }
  ] }
]
