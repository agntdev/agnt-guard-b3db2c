[
  { "name": "verification button must be used in the joined group", "steps": [
    { "send": { "callback": "verify:confirm" }, "expect": [
      { "method": "sendMessage", "payload": { "text": "Open this button in the group you joined." } }
    ] }
  ] }
]
