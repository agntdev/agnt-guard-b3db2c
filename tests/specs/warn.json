[
  { "name": "/warn requires a group admin", "steps": [
    { "send": { "text": "/warn @member repeated links" }, "expect": [
      { "method": "sendMessage", "payload": { "text": "This control is available to group admins only." } }
    ] }
  ] }
]
